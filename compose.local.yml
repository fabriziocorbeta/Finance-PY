# FinancePY — Hosting local (PC de casa) durante fase de prueba, no comercial.
# Datos críticos viven en Supabase (mismo DATABASE_URL que prod) → esta PC solo
# corre compute, no hay Postgres local ni backup de DB que hacer acá.
#
# Uso (dentro de WSL2/Ubuntu con Docker Desktop):
#   cp .env.production.example .env.local && editar valores
#   docker compose -f compose.local.yml --env-file .env.local up -d --build
#
# Networking: Cloudflare Tunnel aparte apunta a http://localhost:8080 (Caddy).
# Ver docs/cloudflared-config.yml.example
#
# Límites de recursos (para no afectar a otras personas usando esta PC):
# Total: ~2.7GB RAM / ~2 cores. Números sincronizados 2026-08-04 con los
# mismos límites ya aplicados a compose.prod.yml (commit 5656158,
# 2026-07-30) para el mismo escenario de PC compartida -- incluye el fix de
# WEB_CONCURRENCY=1 que evita el OOM real encontrado durante el intento de
# Render (Puma en cluster mode con 2 workers no entra en una instancia
# chica). cpu_shares bajo en todos los servicios: en contención, el uso
# normal de la PC gana prioridad sobre estos contenedores.

x-rails-env: &rails_env
  SECRET_KEY_BASE: ${SECRET_KEY_BASE:?SECRET_KEY_BASE requerido en .env.local}
  DATABASE_URL: ${DATABASE_URL:?DATABASE_URL requerido en .env.local (mismo Supabase de prod)}
  SELF_HOSTED: "true"
  RAILS_FORCE_SSL: "false"
  RAILS_ASSUME_SSL: "true"
  APP_DOMAIN: ${APP_DOMAIN:-finance.cd-co.com.py}
  PRODUCT_NAME: ${PRODUCT_NAME:-FinancePY}
  BRAND_NAME: ${BRAND_NAME:-CD & Co.}
  REDIS_URL: redis://redis:6379/1
  ONBOARDING_STATE: ${ONBOARDING_STATE:-closed}
  EXCHANGE_RATE_PROVIDER: ${EXCHANGE_RATE_PROVIDER:-yahoo_finance}
  CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-https://finance.cd-co.com.py}
  CSP_REPORT_ONLY: ${CSP_REPORT_ONLY:-false}
  ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
  OPENAI_ACCESS_TOKEN: ${OPENAI_ACCESS_TOKEN:-}
  OPENAI_URI_BASE: ${OPENAI_URI_BASE:-}
  OPENAI_MODEL: ${OPENAI_MODEL:-}
  ANDROID_WEBHOOK_TOKEN: ${ANDROID_WEBHOOK_TOKEN:-}
  ANDROID_WEBHOOK_FAMILY_ID: ${ANDROID_WEBHOOK_FAMILY_ID:-}

services:
  caddy:
    image: caddy:2
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./Caddyfile.local:/etc/caddy/Caddyfile:ro
    depends_on:
      - web
    networks:
      - financespy_net
    mem_limit: 128m
    cpus: 0.25
    cpu_shares: 256

  web:
    build: .
    volumes:
      - app-storage:/rails/storage
    restart: unless-stopped
    environment:
      <<: *rails_env
      WEB_CONCURRENCY: ${WEB_CONCURRENCY:-1}
    depends_on:
      redis:
        condition: service_healthy
    dns:
      - 8.8.8.8
      - 1.1.1.1
    networks:
      - financespy_net
    mem_limit: 1536m
    cpus: 1.0
    cpu_shares: 512

  worker:
    build: .
    command: bundle exec sidekiq -c 5
    volumes:
      - app-storage:/rails/storage
    restart: unless-stopped
    environment:
      <<: *rails_env
    depends_on:
      redis:
        condition: service_healthy
    dns:
      - 8.8.8.8
      - 1.1.1.1
    networks:
      - financespy_net
    mem_limit: 768m
    cpus: 0.5
    cpu_shares: 512

  redis:
    image: redis:8.8
    restart: unless-stopped
    command: redis-server --maxmemory 200mb --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - financespy_net
    mem_limit: 256m
    cpus: 0.25
    cpu_shares: 256

volumes:
  app-storage:
  redis-data:

networks:
  financespy_net:
    driver: bridge
