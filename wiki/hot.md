---
type: meta
title: "Hot Cache"
updated: 2026-08-20T00:00:00
tags: [meta, hot-cache]
---

# Contexto Reciente

## Última Actualización
2026-08-20. **FinancePY app nativa: wave 1b (Reglas CRUD) cerrada y verificada en dispositivo real, PR #74 (API read-only Budgets/Goals/Receivables de Jules) revisado — encontró bug de seguridad real (`receivables_scope` sin `.accessible_by`, IDOR-adyacente), corregido, mergeado y deployado.** El usuario notó que la app nativa no respeta el design system real de FinancePY (Sure design system, tokens semánticos + Geist) — **decisión: puerto de diseño a Compose bloquea wave 1c (CRUD completo) y wave 1d (wallet-capture)**, spec aún no escrita. 3 prompts CRUD (Budgets/Goals/Receivables) delegados a Jules en paralelo mientras tanto. Detalle: [[Sesión 2026-08-20 FinancePY wave 1b cierre + PR 74 + diseño bloquea 1c-1d]].
**Aparte, mismo día: primer setup de git remoto para este vault (Sistema)** — nunca tuvo remote. Se usa el mismo repo del ERP (`fabriziocorbeta/cd-co-erp`) en rama huérfana nueva `vault` (repo ya aloja `main`=FinancePY y `version-1.1.0`=ERP JS por branch). Excluidos del commit pese a "todo lo untracked": `.local-secrets/` (keystores+backup VM) y `.env`/`.env.local` reales encontrados en subcarpetas — no negociable.

## Actualización anterior
2026-08-12. **FinancePY: login OAuth del RN app (financespy-app, captura Wallet) resuelto en dispositivo real.** Causa raíz no fue ningún bug de código (se hicieron 4 fixes reales igual, ver [[Sesión 2026-08-11-12 FinancePY RN app login OAuth resuelto]]) sino que `compose.local.yml` usa `build: .` y el comando de deploy de toda la sesión (`--force-recreate` sin `--build`) nunca reconstruía la imagen Docker — ningún fix llegó a ejecutarse hasta corregir el comando. Descartes reales en el camino: 0 Cache Rules Cloudflare, cero VMs GCP corriendo (verificado con `gcloud` directo), sin contenedor duplicado; un test que parecía probar "hay otro backend" resultó ser el Service Worker de la PWA sirviendo cache ante un 502 (comportamiento intencional). Descubrimiento colateral importante: hay **3** iniciativas móviles sin decisión tomada entre ellas (TWA/Bubblewrap, Capacitor offline-first, y este RN app) — antes se creía que eran 2. Brainstorming iniciado para decidir cómo conviven.

## Actualización anterior
2026-07-18. **Auditoría de seguridad FinancePY 100% cerrada, CI 100% verde, deploy verificado en prod.** 13 PRs (#51-63) a lo largo de 3 días: seguridad P0-P3, CI reparado de raíz (syntax error + 152 fallas de test triaged, la mayoría causa-raíz única de locale), branch protection, README real, footer sin atribución al fork, y un bug de infra real encontrado en vivo — `compose.prod.yml` nunca pasaba `ONBOARDING_STATE`/`CORS_ALLOWED_ORIGINS`/`CSP_REPORT_ONLY` al contenedor (whitelist explícita, no `env_file`). CSP enforce activo en prod. Diagnóstico de performance real (no reportes IA fabricados): Rails renderiza en 200ms, la lentitud reportada es 100% latencia geográfica (VM en Iowa, usuario en Paraguay) — evaluando mover a São Paulo (`southamerica-east1`) antes de comprometerse al proyecto grande de PWA/procesamiento local. Aparte: **CRÍTICO resuelto** en [[CD & Co ERP]] — IDOR sin autenticar en 9 RPCs Supabase, ver [[CD & Co ERP — IDOR crítico en RPC Supabase (resuelto)]]. Sesión completa: [[Sesión 2026-07-16 FinancePY security fixes deploy]].

## Hechos Clave Recientes
- Negocio central: [[Tienda Online CD & Co]] — relojes online, perfumes próximamente
- Ruta estratégica: tienda (laboratorio) → agentes IA propios → [[Agencias de IA]] productizada
- Recurso clave: [[fable-clon-skill]] — hábitos de Fable 5 para usar en Opus 4.8 (SaaS, subagentes, negocio, contenido redes)
- Fable 5 pasó a pago por uso el 8 jul 2026 ($10/$50 por millón de tokens) — ver [[Fable 5 pago por uso]]
- Ecosistema de soporte definido: [[Proyectos de Claude Chat]] (4 proyectos) + [[Gems de Gemini]] (4 gems); ciclo de retorno "PARA EL VAULT:"
- Emprendimientos activos: [[financespy]] (Rails/GCP), [[CD & Co ERP]] (Supabase/Vercel, hardening RLS+PWA), [[Agente IA CD & Co.]] (WhatsApp), [[CRM Social Hub]]
- 2026-07-16: **FinancePY security P0/P1 resueltos + deployados**. Reporte estaba inflado: de 5 P0 solo 3 accionables (CORS wildcard, CSP off, rate-limit login web); admin-guard y TestController = falsos positivos. PR #51 (commit `1fdcdb2`) mergeado; deploy VM `alejandro-vm` (`/home/Fabrizio/financespy`). Verificado en prod: CORS rechaza origin no permitido, CSP report-only activo, rate-limit login dispara 429. Pendiente: CSP enforce + P2/P3. **CI restaurado** (PRs #52/#53 + rubocop VM): lint/scan_ruby/security-scan verdes; `test` rojo por bugs reales destapados (Money#round ×6, Plaid ×15) — debugging pendiente. Ver [[Sesión 2026-07-16 FinancePY security fixes deploy]]
- 2026-07-14: reporte externo de seguridad FinancePY (75 hallazgos) triaged → [[FinancePY — Auditoría de seguridad y code smells (75 hallazgos)]]

## Hechos Clave Anteriores (2026-05-17)
- [[Agente IA CD & Co.]] — agente WhatsApp dual-mode **código completo**. Pendiente: deploy Railway + env vars reales + webhook Meta
- Stack agente: Claude Haiku 4.5 + prompt caching + Meta Cloud API + Supabase free → costo ~$0.07-0.75/mes
- Dual mode: SECRETARY_PROMPT (clientes, ventas 6 fases) / ASSISTANT_PROMPT (Fabri, datos de negocio)
- Framework ventas: Calificar → Crear Deseo → Manejar Objeciones (5 scripts) → Cerrar (4 técnicas) → Pago → Entrega
- Lead pipeline: capture_lead automático → get_hot_leads → mark_lead_contacted; FAQ router intercepta 20-30% mensajes antes del LLM
- 7 herramientas Claude + 7 tablas Supabase (products, customers, conversations, quotes, payments, deliveries, leads)
- [[CRM Social Hub]] — Fase 1 código completo. Pendiente: crear Supabase + correr schema.sql y seed-auth-trigger.sql + pnpm install + primer signup
- Trigger crítico CRM: `seed-auth-trigger.sql` auto-crea org+profile+pipeline al signup; sin esto RLS rompe
- Stack CRM: Next 15.1 App Router + Supabase SSR + Tailwind + dnd-kit; 9 tablas con RLS por org_id
- **BLOQUEADOR EXTERNO:** verificación Meta Business Manager (3-5 días) — bloquea F2 WhatsApp + F3 IG/FB
- Módulo exportación CD&Co ERP operativo; Pasarelas Paraguay: PagoPar 57/80 preferido

## Cambios Recientes
- 2026-07-07: 12 páginas nuevas (clon Fable, relojes/perfumes/agencia IA, Gems + Proyectos Claude) + sesión guardada
- 2026-07-06: ingest de clon.pdf → [[fable-clon-skill]]
- 2026-05-17: CRM F1 hardening + Agente IA CD&Co implementado

## Hilos Activos
- **FinancePY — 3 iniciativas móviles sin decisión tomada, brainstorming en curso (2026-08-12)**: TWA/Bubblewrap (wrapper simple, sin offline), Capacitor offline-first (rama `feature/android-offline-phase1` en `cd-co-erp`, sin mergear), y React Native `financespy-app` (captura Wallet + OAuth, login recién resuelto, rama `feature/financespy-mobile-app` en `financespy`, sin mergear). Ninguna sabía de las otras dos al construirse. Detalle: [[Sesión 2026-08-11-12 FinancePY RN app login OAuth resuelto]].
- **FinancePY — webhook de notificaciones Wallet: dos bugs reales arreglados, bloqueado en decisión de plataforma (2026-08-10)**: arquitectura correcta (Tasker/MacroDroid → webhook Rails, no la app leyendo notificaciones). Dos bugs de servidor confirmados y arreglados en prod: (1) `ANDROID_WEBHOOK_TOKEN` nunca migró de la VM a `.env.local` de la notebook → 503 constante; (2) formato real de monto en notificación PYG es coma-miles (`"112,000"`, confirmado con notificación real), el primer fix (`5b1daba`, separador hipotético con punto) lo corrompía — corregido en `f20fa8d`. Mapeo de 4 cuentas Wallet↔FinancePY confirmado. **Bloqueante actual**: Fabrizio exige automatización 100% nativa (sin Tasker/MacroDroid) — investigado a fondo con capturas reales del dispositivo, confirmado que Samsung "Modos y Rutinas" (y Android nativo en general, ningún fabricante) no tiene forma de leer texto de notificación como variable ni acción HTTP/webhook. No hay tercera vía nativa oculta. Opciones reales: MacroDroid (gratis, diseño completo listo) vs. app Android propia en Kotlin (desarrollo real, sin diseño armado) vs. carga manual. Fabrizio investiga por su cuenta antes de decidir. Detalle completo en [[FinancePY - Spec Módulos Neto (recurrentes, notificaciones, inversiones)]].
- **FinancePY — el shell Android es TWA, no Capacitor (2026-08-08)**: el spec del 05/08 eligió Capacitor pero lo buildeado el 06-07/08 fue **TWA Bubblewrap** (`~/code/financespy-twa`, APK firmada, `py.com.cd_co.finance.twa`). Manifest declara `POST_NOTIFICATIONS` + `DelegationService` (mostrar push), cero `BIND_NOTIFICATION_LISTENER_SERVICE`. Trampa: `enableNotifications: true` en `twa-manifest.json` = mostrar, NO leer. Un TWA no puede leer notificaciones ajenas — pero no importa, el diseño real delega eso a Tasker. **Contradicción cerrada de documentar, no de decidir**: la sesión del 05/08 (Capacitor) se recuperó y guardó — vivía sin pushear en un worktree de `alejandro-vm`, que se había apagado por la migración. Se prendió la VM un momento, se pusheó `feature/android-offline-phase1` a GitHub, se apagó de nuevo (nada quedó corriendo). Esa rama trae endpoints `/sync/transactions` + cliente IndexedDB **verificados en vivo** (74 transacciones reales, cero escrituras de prueba en prod) — la capa de datos offline sirve sin importar qué shell gane. Detalle: [[Sesión 2026-08-05 FinancePY offline-first Fase 1 (Capacitor)]]. **Pendiente real: decidir TWA vs Capacitor a propósito**, nadie lo hizo todavía.
- **FinancePY — reporte Gemini "módulos Neto" auditado, mayormente redundante (2026-08-08, CERRADO)**: propone construir recurrentes, portfolio, presupuestos, metas y gate Pro — ~40 ítems ya existen en el código con modelo+UI+tests (`recurring_transaction.rb` con detección automática de patrones, `investment.rb`/`holding.rb` con cripto y 10+ providers de cotización, `budget.rb`/`goal.rb` completos, `subscription.rb` con Stripe end-to-end). Auditoría de 4/7 áreas con verificación adversarial (workflow, 3 se colgaron por timeout, completadas a mano). Faltantes reales encontrados que NO estaban en el reporte: bug de `GoalPledge::Reconciler` (pledges huérfanos), `es-PY` inalcanzable en UI, fallback i18n roto, y el bloqueante comercial real — Stripe no opera en Paraguay, falta provider local. Backlog priorizado por esfuerzo/valor en [[FinancePY - Spec Módulos Neto (recurrentes, notificaciones, inversiones)]].
- **FinancePY — hosting MIGRADO Y EN PRODUCCIÓN (2026-08-08)**: la migración se **ejecutó**. Corre en notebook ASUS (i5 7ma gen, 8 GB RAM) bajo WSL2 + Docker (`compose.local.yml`, 4 servicios) + Cloudflare Tunnel, sirviendo `finance.cd-co.com.py`. **VM GCP apagada** — cualquier comando `gcloud compute ssh alejandro-vm` de sesiones anteriores ya no aplica. DB sigue en Supabase. Pendiente: arranque automático al boot ([[FinancePY — Arranque automatico y fallback (notebook WSL2)]], guía lista sin aplicar — WSL no arranca solo, `cloudflared` quedó como SysV sin supervisor). **Restricción nueva: 8 GB RAM con ~3,1 GB ya usados** — dimensionar antes de sumar jobs periódicos.
- **FinancePY — auditoría de seguridad 100% cerrada (2026-07-18)**: 13 PRs (#51-63), CI 100% verde, CSP enforce activo en prod, registro cerrado, footer limpio. Ver sección "Continuación — cierre completo" en [[Sesión 2026-07-16 FinancePY security fixes deploy]] para el detalle completo.
- **FinancePY — entorno dev local no anda en este Mac (2026-08-05)**: repo en `~/code/financespy`, pero falta ruby correcto (sistema trae 2.6.10, no matchea `.ruby-version`), bundler, Postgres local y Docker Desktop no estaba corriendo. `compose.local.yml` es alternativa pero comparte DB con prod (Supabase), no aislado. Detalle: [[FinancePY — entorno dev local bloqueado en Mac]].
- **FinancePY — pendiente técnico real**: `test/system/settings_test.rb` flaky bajo paralelización (mock leak, confirmado 3+ veces sin relación a ningún diff — backlog estabilizar el mock); `Holding::Gapfillable`/`Balance::Materializer` necesitan profiling real antes de optimizar (dataset actual mínimo, no urgente); resto de P3 (schema changes, refactors grandes) sin tocar.
- **VM alejandro-vm — CONFIRMADO apagada a propósito (2026-08-08, cierra el hilo anterior)**: se apagó con snapshot manual (`alejandro-vm-manual-20260807`) + backup de `.env`/compose de alejandro-agent/n8n en `.local-secrets/`. Alejandro (n8n + alejandro-agent + openwa-api) estaba desactivado por reconfiguración pendiente al momento del apagado — no está sirviendo clientes desde ahí ahora mismo. Snapshot diario automático confirmado funcionando. Pendiente real: dónde y cuándo se reactiva el [[Agente IA CD & Co.]] (Ryzen3/16GB dedicada, según plan del 05/08 — sin confirmar si ya se ejecutó ese realojo).
- **CD & Co ERP Supabase**: revisar si hay más RPCs con el mismo patrón IDOR fuera de las 14 SECURITY DEFINER ya auditadas (financespy schema ya verificado limpio, cero funciones custom); 185 unused_index sin revisar; activar `auth_leaked_password_protection` (manual, sin tool MCP disponible)
- **Agente IA CD&Co** — deploy: Railway + env vars + webhook Meta + test con número real
- **CRM Fase 1** activación: Supabase + SQLs + pnpm install + primer signup
- **Meta Business Manager** verificación: iniciar YA (bloqueante F2/F3 CRM + agente)
- Crear los 4 Gems ([[Gems de Gemini]]) y 4 Proyectos claude.ai ([[Proyectos de Claude Chat]])
- Cargar fable-clon-skill como skill/CLAUDE.md en los proyectos SaaS
- CD&Co ERP: probar importación real CSV formato FinancePY; pasarela: PagoPar Avanzado candidato
- Ingestar datos reales de la tienda (márgenes, catálogo) para reemplazar rangos genéricos
