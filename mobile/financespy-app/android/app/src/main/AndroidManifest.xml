<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      <!--
        No manual intent-filter for the "financespy" scheme here: react-native-app-auth
        registers its own net.openid.appauth.RedirectUriReceiverActivity for that scheme,
        driven by the manifestPlaceholders.appAuthRedirectScheme value in build.gradle.
        A second matching intent-filter on MainActivity previously collided with it:
        Android had two activities claiming the same financespy://oauth/callback intent,
        and the OAuth redirect could be captured by MainActivity instead of AppAuth's own
        receiver, leaving react-native-app-auth's authorize() promise never resolving
        (confirmed live: login hung on the browser screen after tapping Authorize).
      -->
      <service
          android:name=".WalletNotificationListenerService"
          android:label="FinancePY Wallet Listener"
          android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
          android:exported="false">
          <intent-filter>
              <action android:name="android.service.notification.NotificationListenerService" />
          </intent-filter>
      </service>
    </application>
</manifest>
